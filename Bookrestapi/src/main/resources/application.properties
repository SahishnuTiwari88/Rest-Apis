server.port=8082
spring.datasource.name=api
spring.datasource.url=jdbc:mysql://localhost:3306/api
spring.datasource.username=root
spring.datasource.password=Sarvesh143
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update


#Multipart configuration for uploading files on database or folder
#Enabling multipart upload

spring.servlet.multipart.enabled=true

#max file size that can be uploaded
spring.servlet.multipart.max-file-size=300MB

#Threshold after which files are written to disk(Minimum file size that can be uploaded)
spring.servlet.multipart.file-size-threshold=1KB 
